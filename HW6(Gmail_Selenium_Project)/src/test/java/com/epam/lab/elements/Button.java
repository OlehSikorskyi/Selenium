package com.epam.lab.elements;

import org.openqa.selenium.WebElement;

public class Button extends AbstractElement.Element {
    public Button(WebElement webElement) {
        super(webElement);
    }
}